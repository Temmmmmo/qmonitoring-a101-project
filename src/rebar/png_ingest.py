"""Стадия A (позже) — разбор мозаики из PNG в Mosaic.

Делать ПОСЛЕ рабочего dxf_ingest. Идея (docs/context.md §2.4):
  1. прочитать полосу-легенду сверху изображения -> упорядоченные RGB;
     сопоставить с подписями из .shk по порядку -> легенда;
  2. восстановить масштаб (габариты / расстояние между строительными осями) -> мм/пиксель;
  3. квантовать пиксели поля к ближайшему RGB легенды (не CV-кластеризация — цвета известны);
  4. если сетка КЭ доступна (линии/версия '+значения') — агрегировать по КЭ голосованием
     большинства; иначе работать на регулярной решётке.

Возвращает тот же тип Mosaic, что и dxf_ingest — единый контракт вниз по пайплайну.
"""

from __future__ import annotations

from .models import Mosaic


def read_mosaic_png(png_path: str, shk_path: str | None = None) -> Mosaic:
    raise NotImplementedError("TODO стадия A (позже): разбор PNG -> Mosaic")
