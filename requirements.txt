# Core
ezdxf>=1.1          # DXF parsing (3DFACE, blocks, ACI colors -> RGB)
openpyxl>=3.1.5,<4  # read-only LIRA numeric XLSX import
defusedxml>=0.7.1,<1 # protect XML parsing in uploaded XLSX files
fastapi>=0.115       # local MVP web API and static frontend
httpx>=0.28          # FastAPI TestClient
numpy>=1.26
scipy>=1.11        # composite coverage and batch cutting MILP
python-multipart>=0.0.20  # multipart DXF/.shk uploads
shapely>=2.0        # geometry (zones, rectangles) — used from stage B onward
uvicorn>=0.34        # local ASGI server

# PNG ingest path (stage A, fallback source)
opencv-python>=4.9
Pillow>=10.0

# Optimization (stage C/D — later)
# pulp>=2.8
# ortools>=9.9

# Dev
pytest>=8.0
# Точная версия не даёт локальному запуску и CI использовать разные наборы правил.
ruff==0.14.8
