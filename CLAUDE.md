# CLAUDE.md — Qmonitoring × A101 хакатон

## Что это за проект
Хакатон-кейс от девелопера **A101** (площадка «Техлаб Москва»). Задача: **автоматизировать раскладку дополнительного армирования** железобетонных плит.

Сейчас процесс ручной: инженер получает из **ЛИРА-САПР** цветовую мозаику (изополя) требуемой интенсивности армирования → вручную переносит зоны в проект. Нужно заменить это алгоритмом.

Артём (владелец этой папки) — Python backend (Яндекс, middle+), физфак МГУ. В команде числится **ведущим ML-специалистом**. ML-опыт минимальный; цель проекта — прокачать ML/DS/CV на реальном кейсе, параллельно ведя алгоритмическое ядро. Backend/деплой/архитектура — его сильная сторона.

Команда: Кирилл (аналитик), Даниил/@PoleschukDB (техдир Qmonitoring, консультант), Дарья (ML), + Revit-специалист (позже). Заказчик-контакт: @AVFroym.

## Задача в одну фразу
Вход: 4 файла на плиту (DXF или PNG) = 4 направления армирования: **{Нижнее, Верхнее} × {ось X, ось Y}**. Ось X — горизонтальная, Y — вертикальная.
Выход: набор **прямоугольных участков** доп. армирования (стержни), оптимальный по расходу стали, → JSON → **плагин Autodesk Revit** строит стержни и чертежи.

## Формат исходных данных (проверено на файлах)
- **DXF (основной источник, точный):** мозаика = ~4200 сущностей `3DFACE`, по одной на конечный элемент (КЭ) сетки. Цвет = ACI color index. Геометрия в **метрах** (bbox 0..23.6 × 0..14 — совпадает с размерами на PNG). ⚠️ ТЗ на стр.9 говорит «DXF в миллиметрах, ×1000» — но файлы в метрах. **Уточнить у наставников.** Скорее всего ×1000 нужно для приведения в мм при построении в Revit.
  - Легенда: блок `KLEENKA` (9 `SOLID` с индексами цветов) + порядок подписей во внешнем `.shk`. Маппинг цвет→спецификация по позиции в шкале.
- **PNG (fallback, требует CV):** тот же рисунок растром. Есть версии `+значения` (с числами). Нужно: восстановить масштаб (по расстоянию между строительными осями или по общим габаритам), квантовать цвета к палитре легенды.
- **.shk** — бинарный файл цветовой шкалы ЛИРА. Содержит строки-подписи диапазонов: `s300d18`, `s300d18+s150d20`, ...
- **.dwg / .pdf** в `Для верификации изополей/` — **эталонные ответы инженеров** = наш test/eval set.

## Как читать подпись армирования
`s300d18` = **фоновое** армирование (шаг 300 мм, диаметр 18 мм).
`s300d18+s150d20` = фон + **дополнительное** s150d20 (шаг 150, d20). **Раскладываем именно дополнительную часть.**
- Диаметр `d` доп. стержня → анкеровка = **≥40·d** с каждой стороны за границу зоны.
- Шаг `s` → ширина участка кратна шагу; число стержней = ширина/шаг (+1).
- Масса стержня ≈ `0.006165 · d² · длина` кг (d в мм, длина в м). d18≈2.0, d20≈2.47, d25≈3.85 кг/м.

## Инженерные ограничения (из ТЗ)
1. Участок — прямоугольник, ориентирован вдоль направления стержней (X или Y).
2. Мин. ширина зоны = **2–3 КЭ мозаики** по ширине.
3. Ширина кратна шагу стержней.
4. Длина = протяжённость мозаики вдоль стержня **+ 2× анкеровка (≥40d)**.
5. Зону неправильной формы можно **разбивать на несколько участков разной длины** ради экономии стали.
6. Правило «одиночного КЭ»: если в зоне ровно 1 КЭ данного цвета — его можно игнорировать, взять предыдущий диапазон по шкале.
7. **Коллизии:** соседние участки раздвигать на ≥ шаг (пример: зазор 100 мм), чтобы крайние стержни не совпали.
8. **Оптимум = «Точка 3»** на кривой масса↔кол-во деталей: колено кривой, где рост числа деталей уже не снижает расход стали. Стратегия по умолчанию.
9. Проверка по стандартным таблицам (Таблица 2.4.2): допустимые (d, шаг) для типа плиты; строки «не допускается» (превышен предельный % армирования, СП63.13330.2018) отбраковывать.

## Целевой pipeline (черновик архитектуры)
```
DXF/PNG ─▶ [A] Ingest ─▶ общая модель: КЭ{геометрия, требуемое доп.As/спека, направление}
         [B] Segmentation: зоны по уровням интенсивности (связные компоненты, вложенные контуры)
         [C] Rectangularization: покрытие зон прямоугольниками (мин.ширина, кратность шагу, разбиение)
         [D] Detailing+Optim: длина+анкеровка, масса, разведение коллизий, поиск «Точки 3»
         [E] Export JSON ─▶ Revit-плагин (C#/pyRevit): стержни+аннотации+чертёж на активном виде
```
Эталон из `Для верификации` → метрики: покрытие требуемой площади, перерасход стали vs эталон, число деталей.

## Стек
Python 3, ezdxf (DXF), OpenCV/NumPy (PNG CV), shapely (геометрия), возможно PuLP/OR-tools (оптимизация покрытия). Revit-часть — C# Revit API или pyRevit, отдельным специалистом.

## Соглашения
- Не коммитить тяжёлые бинарники (PNG/DXF/DWG) в git без нужды.
- Все размеры внутри ядра — в **мм**, направление — enum. Единицы фиксировать на границе ingest.
- Тесты гонять на `Для верификации изополей/` (реальные кейсы, есть эталон).
- Прогресс и решения писать в память Claude (см. `~/.claude/.../memory/MEMORY.md`), чтобы не терять нить между чатами.
