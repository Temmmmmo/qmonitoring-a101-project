# Архитектура оптимизации

Статус: MVP-контракт, обновлённый после встречи 2026-08-20. Инженерные допущения и открытые вопросы находятся в
[`optimization-notes.md`](optimization-notes.md).

Дополнение 13 сентября, автоматическая физическая постобработка:
[`automatic-assistant-pipeline-2026-09-13.md`](automatic-assistant-pipeline-2026-09-13.md).
`assistant_inputs` получает свежий полный GA-вариант; `physical_layout_recovery`
оркестрирует точные схемы СТО, `physical_normalization` и независимый экспорт.
Нормализатор использует отдельные typed `PhysicalSourceBar/PhysicalBar`, не
импортирует application/scripts/artifacts и не меняет `LayoutZone`.
Параметрические зоны остаются первичным проектным результатом; many-to-one
физические владельцы передаются отдельным `physical-bar-plan-trial/v1`.
Один `prepare_revit_assistant.py` собирает пакет без ручного выбора accepted raw;
старые API/defaults и обязательный откат Revit сохраняются.

Дополнение 13 сентября: [полный составной web/CLI](composite-plate-workflow-2026-09-13.md).
`analyze_composite_plate` объединяет четыре DXF, явные фазы, все компоненты шкал,
составной MILP-пул и общеплитный DP `combine_keyed_candidates`. Отдельный endpoint
`/api/analyze-composite-plate` не ослабляет старые однокомпонентные API. Геометрия
сетки проверяется по каждому КЭ; ни одного направления или края нельзя отбросить.

`stock_cutting` — независимая проверка заданной ведомости. `stock_length_balance`
— отдельный MILP выбора длин компонентов и целочисленных схем раскроя партии.
Профиль `plate-11700-batch` разрешает общему детализатору явные каталожные длины
не меньше минимальных; остальные профили сохраняют прежний строгий минимальный
выбор. Успешный подбор возвращается через тот же детализатор и coverage/host,
не подменяет физическую геометрию одним улучшенным числом в отчёте.
Сериализация/UI остаются вне алгоритмов; `models.py` и пакет Revit v2 не меняются.

Дополнение 12 сентября: [новые исходники и план](post-meeting-materials-2026-09-12.md).
`services/bar_schedule.py` группирует прямые стержни и независимо считает их массу;
`services/position_combinations.py` объединяет множества типоразмеров четырёх
направлений. Для `position_count` локально доминируемый кандидат может быть полезен
всей плите, поэтому `DirectionParetoFront.combination_candidates` сохраняет весь
переданный hard-valid набор. MILP использует переменные активации типоразмеров;
общеплитный DP хранит минимальную массу для каждого множества и явно отказывает
при превышении лимита памяти. Это не доказательство глобального оптимума за пределами
генерируемых кандидатов. Старые две оси сохраняют прежний аддитивный алгоритм.

Web выбирает `position_count` по умолчанию; CLI/низкоуровневые функции сохраняют
прежние defaults для воспроизводимости. В приложении preprocessing по умолчанию
`preserve`, прямой вызов старого сервиса оставлен совместимым как `legacy-research`.
Числовой [XLSX-ingest](lira-excel-import.md) реализован отдельно от цветового `Mosaic`:
`LiraPlate -> явная шкала/оси -> DemandMap`. Он сохраняет точное As в происхождении
каждого КЭ и проверяет среднюю площадь рецепта, а не только порог шкалы.
Преобразования СТО и полный составной сценарий ещё не подключены к этому входу.

Дополнение 7 сентября 2026: [составные схемы и фактические оси](reinforcement-contract-v2-2026-09-07.md).
С согласия пользователя добавлены `Band.recipe` / `DemandLevel.recipe`, отдельные
`CompositeLayoutZone` и `RecipePlacement`. Старый `LayoutProblem -> LayoutSolution`
остаётся однокомпонентным и явно отклоняет составные шкалы. Новая детализация выбранной
составной зоны не является новым GA и не подменяет проверку инженерной допустимости.

Дополнение 9 сентября: `application/core_revit_trial.py` оборачивает существующий
экспорт одной зоны v2 в **отдельную test-only привязку** к эталонному host.
Геометрия/длина/масса вычисляются общими сервисами, pyRevit не импортируется ядром.
Плагин `Core Trial` независимо проверяет пакет и физическое чтение двух регулярных
рядов после Commit; вся группа всегда откатывается. Это перенос синтетической зоны
из конструктора, не сквозной составной GA и не обход `export_eligible=false`.

Повторная сверка геометрии зон с готовыми КЖ выявила необходимость отделить логические
прямоугольники от осей стержней и установленной длины. После встречи overlap зон
разрешён, а `40d`/раздвижка оформлены как общая постобработка. Обоснование, локальные
источники и оставшиеся допущения описаны в
[`layout-research-2026-08-19.md`](layout-research-2026-08-19.md).

Обзор всех реализованных методов, их пошаговой работы и Mermaid-схемы проекта находятся
в [`algorithms.md`](algorithms.md).

## Главный принцип

Парсер и оптимизаторы не зависят друг от друга напрямую:

```text
DXF -> Mosaic -> mapping? -> адаптер -> LayoutProblem -> LayoutOptimizer -> demand zones
                                                       ↓
                 общая постобработка + validator -> LayoutZone[] -> LayoutSolution
                                                                    ↓
                                                               JSON/Revit
```

- `Mosaic` — стабильный публичный контракт результата ingest;
- `mapping` — отдельное обогащение `Band` для DXF без `.shk`, только по явно выбранной
  и проверенной таблице;
- `LayoutProblem` — единый вход всех алгоритмов;
- `LayoutSolution` — единый выход для валидации, JSON и HTML;
- алгоритм выбирается по стабильному имени через `OptimizerRegistry`;
- масса после `40d`/раздвижки, покрытие и нарушения пересчитываются общим
  `evaluate_layout`, а не самим
  алгоритмом. Поэтому результаты можно сравнивать на одинаковых правилах.

Глобальный оптимизатор выбирает прямоугольные зоны, потому что это прямо требуемый ТЗ
результат. Внутренняя целевая функция пока штрафует число зон; соответствие этого
счётчика слову «деталь» на графике ТЗ требует подтверждения. Для каждой зоны общие
сервисы постобработки вычисляют ширину, длину с `40d`, диаметр, шаг, фазу, количество
стержней и итоговую массу.
Revit-плагин затем размещает выбранное семейство по этим параметрам.

## Структура пакета

```text
src/rebar/optimization/
  contracts/
    problem.py       DemandMap, ограничения и LayoutProblem
    result.py        запрос, детали, метрики и LayoutSolution
  adapters/
    mosaic.py        Mosaic -> DemandMap -> LayoutProblem
  mappings/
    contracts.py     формат версионированной таблицы As -> арматура
    binder.py        проверка шкалы и создание Band без мутации исходного Mosaic
    plate_zero.py    MVP-таблица plate-zero-d12-v1 из легенд PNG
    k09_minus_2.py   узкая MVP-таблица плиты над −2 этажом
    k09_above_3.py   узкая MVP-таблица только плиты над 3 этажом
  services/
    geometry.py      чистые геометрические примитивы
    detailing.py     build_zone для групп КЭ и build_zone_from_bbox для spatial-разбиения
    evaluation.py    независимая проверка зон, объединённого покрытия, зазоров и метрик
    anchorage.py     заменяемая политика длины анкеровки
    cutting.py       допустимые длины отрезков для конкретного проекта
    bar_geometry.py  фаза и разворачивание зоны в линии стержней
    phase.py         подбор общей фазы и устранение физических конфликтов
  algorithms/
    base.py          протокол LayoutOptimizer
    _guillotine.py   общая генерация допустимых ортогональных разрезов
    bbox.py          одна сильная охватывающая деталь — грубый baseline
    bsp.py           рекурсивное ортогональное разбиение — baseline
    greedy.py        жадное объединение поперечных полос — быстрый baseline
    greedy_priority.py  разрезы с приоритетом дорогих цветовых уровней
    agglomerative.py  bottom-up слияние связных пятен уровней
    row_run_greedy.py жадное объединение продольных профилей спроса
    strip_profile_dp.py DP по последовательным поперечным полосам
    spatial_partition_greedy.py сеточная маска и укрупнение непересекающихся зон
    genetic_pareto.py NSGA-II-подобная оркестрация и материализация фронта
    genetic/
      operators.py   split/merge/shift/change-level/baseline-patch
      policy.py      uniform и online UCB1 выбора мутаций
  registry.py        явная регистрация и выбор алгоритма
src/rebar/golden/
  catalog.py         проверенный инженерный эталон и страницы PDF
  sources.py         поиск локальных материалов без абсолютных путей
  pdf_specs.py       извлечение спецификаций и превью страниц
src/rebar/reporting/
  zone_schedule.py   стабильные марки и выноски параметрических зон
src/rebar/application/
  revit_export.py    компактный draft JSON только выбранного plate-кандидата
```

`src/rebar/optimization/__init__.py` — единственная рекомендуемая точка импорта для
потребителей. Внутреннюю структуру можно менять без переписывания CLI и отчёта.

Локальная визуализация вынесена из оптимизации в `src/rebar/reporting/`, а исполняемый
CLI остаётся тонким файлом `scripts/compare_optimizers.py`.

Локальный web-MVP использует ту же границу:

```text
POST /api/analyze
       ↓
application.analyze_direction
       ↓
DXF ingest -> mapping? -> LayoutProblem -> OptimizerRegistry -> LayoutSolution[]
                                                               ↓
                                      standards validation -> reporting / export
```

`src/rebar/application/` оркестрирует один use case, но не содержит геометрических
правил. `src/rebar/web/` отвечает только за временный upload, HTTP-валидацию,
сериализацию и UI. Поэтому будущие CLI, публичный API и Revit-интеграция смогут вызывать
тот же сценарий без копирования алгоритмов. Встроенный demo-case создаётся как настоящий
синтетический DXF в `application/demo.py` и проходит тот же путь. Загруженные web-файлы
сейчас не сохраняются.

`application/revit_export.py` является отдельной выходной границей: он принимает уже
выбранный и проверенный `PlateSolution`, добавляет ведомость марок и создаёт компактный
`plate-solution-revit/v1` без web-SVG и остальных кандидатов. Пока safety-гейты не
закрыты, контракт остаётся `draft` и не выдаёт решение за готовое к применению.

`src/rebar/standards/` хранит версионированную транскрипцию таблиц А101 отдельно от
mapping и алгоритмов. Mapping выбирает профиль и переносит его ID в `DemandMap.meta`;
общий `evaluate_layout()` запрещает явно красные позиции после генерации кандидата.
Application-слой тем же валидатором формирует гейт `pass/fail/not_checked`, поэтому
эвристика не может обойти каталог собственной оценкой допустимости.

`src/rebar/learning/preference.py` расположен после Pareto/hard-validation. Он принимает
только метрики допустимых `PlateCandidate`, учит один вес ранжирования и не может
создавать зоны или менять инженерные ограничения. JSON/HTML этого эксперимента остаётся
в `reporting/preference_calibration.py`.

## Контракт алгоритма

Каждая реализация обязана иметь стабильное `name` и один метод:

```python
def solve(
    self,
    problem: LayoutProblem,
    request: AlgorithmRequest | None = None,
) -> LayoutSolution:
    ...
```

Алгоритм отвечает только за выбор разбиения. Он должен использовать общую детализацию и
не должен подменять правила оценки локальной формулой ради более выгодных метрик.

Метод, который естественно создаёт популяцию вариантов, дополнительно реализует:

```python
def solve_many(
    self,
    problem: LayoutProblem,
    request: AlgorithmRequest | None = None,
) -> tuple[LayoutSolution, ...]:
    ...
```

Это контракт `LayoutCandidateGenerator`. Application-слой сохраняет один центральный
представитель для обратной совместимости, но передаёт всю серию общему hard-валидатору
и Парето-фильтру. `genetic-pareto` является первой такой реализацией.

Текущий выходной контракт остаётся на уровне зоны:

```python
@dataclass(frozen=True)
class LayoutZone:
    id: str
    bbox: BBox
    demand_bbox: BBox
    level_index: int
    rebar: Rebar
    width_mm: float
    required_length_mm: float
    anchored_length_mm: float
    installed_length_mm: float
    first_bar_coordinate_mm: float
    bar_count: int
    mass_kg: float
```

При `width_mm = k × step` количество равно `k + 1`, а масса считается по одинаковой
`installed_length_mm` всех стержней зоны. Область спроса, минимальная длина с анкеровкой,
принятая длина отрезка и первая поперечная ось разделены явно. Линии отдельных стержней
вычисляются как `first + i × step`; отдельный публичный `PlacedBar[]` не нужен.

## Уровни решения и метрик

Текущий `LayoutProblem -> LayoutSolution` описывает **одно направление** армирования.
Его `metrics.detail_count` исторически равен числу `LayoutZone`, а не числу стержней
внутри них. В интерфейсе эту величину следует называть `zone_count`, пока терминология
графика ТЗ не подтверждена.

Для любой раскладки уже можно вычислить второй показатель без нового доменного типа:

```text
physical_bar_count = sum(zone.bar_count for zone in solution.zones)
```

Число позиций спецификации требует отдельной группировки стержней по геометрии,
диаметру и длине. Оно не равно автоматически ни числу зон, ни числу стержней.

На встрече подтверждено, что график «Точка 3» агрегирует четыре направления. Поэтому для
полного сценария нужен следующий слой в пакете оптимизации:

```text
PlateProblem
├── bottom-X: LayoutProblem
├── bottom-Y: LayoutProblem
├── top-X:    LayoutProblem
└── top-Y:    LayoutProblem

PlateSolution
├── direction_solutions: 4 × PlateDirectionSolution(direction + LayoutSolution)
├── zone_count: sum(direction.detail_count)
├── physical_bar_count: sum(zone.bar_count)
├── total_mass_kg: sum(direction.total_mass_kg)
└── diagnostics/status
```

Базовые контракты реализованы в `optimization/contracts/plate.py`; общая агрегация и
распространение невалидности любого направления находятся в
`optimization/services/plate.py`. `application/analyze_plate.py` принимает ровно четыре
источника, проверяет уникальность направлений и строит по одному общеплитному baseline
на каждый выбранный алгоритм. Публичный ingest-контракт `models.py` не менялся.
Web-граница может передать каждому `PlateDirectionSource` один и тот же явно загруженный
`.shk`; `read_mosaic` всё равно независимо проверяет число полос каждого DXF. При
несовместимости весь plate-запуск отклоняется до построения фронта.

Слой конечных кандидатов реализован отдельно:

```text
LayoutSolution[] × 4
  -> evaluate_layout() повторно для каждого кандидата
  -> DirectionParetoFront × 4
  -> комбинации разных алгоритмов по направлениям
  -> PlateSolution[]
  -> PlateParetoFront(mass, explicit complexity axis)
```

Контракты находятся в `optimization/contracts/front.py`, прозрачные метрики — в
`services/constructability.py`, Pareto-pruning — в `services/pareto.py`. Недопустимый
статус или ошибка hard-валидатора исключают вариант до сравнения. Равные точки
схлопываются с сохранением идентификаторов альтернатив. Сейчас источники дают по одному
финальному решению на алгоритм, поэтому этот фронт честный внутри переданного конечного
набора, но не является доказанным глобальным фронтом.

Геометрические задачи четырёх направлений решаются независимо. Каждый кандидат обязан
сохранять `zone_count`, `physical_bar_count` и `total_mass_kg` отдельно. Для построения
общего фронта выбирается явная метрика сложности: сейчас `zone_count` или
`physical_bar_count`. Дополнительно сохраняются уникальные диаметры, шаги, установленные
длины, сигнатуры раскладки и число предупреждений. Веса для этих компонентов не
придумываются. До подтверждения терминологии графика нельзя молча называть
`detail_count` числом деталей заказчика.

`AlgorithmRequest.max_details` сейчас относится только к одному `LayoutSolution`.
Значение `4` в примере README — ручной smoke-test, а не найденная «Точка 3» и не итог
всей плиты.

Чтобы добавить следующий алгоритм, например `cpsat`:

1. создать отдельный файл `optimization/algorithms/cpsat.py`;
2. реализовать протокол без изменения `Mosaic` и существующих контрактов;
3. зарегистрировать класс в `built_in_optimizer_registry()`;
4. добавить общий поведенческий тест в `tests/optimization/test_algorithms.py`;
5. после этого имя автоматически можно передать в локальный отчёт через `--algorithms`.

Генетический оптимизатор реализован публичным `genetic_pareto.py` и внутренним пакетом
`algorithms/genetic/`. Хромосома выбирает прямоугольники из пространственного
CandidateSet, дополненного геометриями сильных frozen-baseline. UCB1 адаптивно выбирает
один из фиксированных предметных операторов, но crossover/mutation не получают
права создавать непроверенный инженерный результат: после них обязательны
детерминированный repair, общая постобработка и `evaluate_layout`.

До любого оптимизатора application-слой вызывает `apply_single_cell_rule()`. Сервис
возвращает новый `LayoutProblem`, не изменяет `Mosaic` и кладёт полный протокол понижений
в `problem.meta`. Там же `resolve_zone_count_limit()` заменяет отсутствующий cap на
естественный максимум: `1..число всех КЭ` для непустого спроса и `0..0` для пустого.
Внешний Парето-фильтр для двух целей работает после сортировки за `O(n log n)`, поэтому
общеплитное произведение кандидатов больше не попадает в квадратичный dominance-loop.

ML выбора «Точки 3» находится над `PlateSolution`, а не внутри геометрического
валидатора. Локально доступно 11 объединённых инженерских выдач и 0 явно размеченных
точек колена, поэтому первая модель должна оценивать один параметр предпочтения и
проверяться leave-one-project-out.

## Текущие ограничения MVP

`bbox`, `bsp`, `greedy`, `greedy-priority`, `agglomerative`, `row-run-greedy`,
`strip-profile-dp`, `spatial-partition-greedy` и `genetic-pareto` — сравнимые
экспериментальные методы, а не готовая инженерная выдача. Последний строит приближённый
многоточечный фронт внутри конечного CandidateSet и не заявляет глобальный оптимум.
Greedy сначала строит непересекающиеся поперечные полосы минимальной ширины, затем
объединяет соседние пары с наименьшим приростом целевой стоимости. Он быстрый и всегда
покрывает спрос в текущей модели, но не рассматривает произвольные прямоугольники и не
гарантирует глобальный оптимум.

`greedy-priority` использует те же допустимые гильотинные разрезы, что и BSP, но оценивает
их не только по немедленному снижению целевой функции. Дополнительный приоритет равен
оценке массы перерасхода, которую удаётся убрать, если сильный уровень больше не
назначается слабым КЭ. Вес задаётся `AlgorithmRequest.params["priority_weight"]`, по
умолчанию `1.0`. Это помогает изолировать дорогие уровни, но не снимает минимальную
ширину и обязательную общую постобработку.

`agglomerative` идёт в обратную сторону: начинает со связных по bbox пятен одного уровня
и сливает ближайшие зоны. Сначала устраняются обязательные конфликты, затем число зон
доводится до `max_details`. Это быстрый bottom-up baseline; связь через bbox и выбор
ближайшей пары являются эвристиками.

`spatial-partition-greedy` принципиально не группирует произвольные КЭ перед построением
bbox. Он дискретизирует область с шагом порядка медианного размера КЭ, строит
непересекающиеся начальные прямоугольники и затем укрупняет ортогональных соседей.
Слияние разрешено, только если весь новый сеточный bbox состоит из плиток с геометрией
`KLEENKA`; частично пересекающиеся активные зоны замыкаются в один прямоугольник.
Решение хранит полную жадную траекторию и недоминируемые точки внутри неё. Это создаёт
реальное множество вариантов для MVP, но не доказывает глобальную оптимальность и
зависит от разрешения сетки.

Общие ограничения baseline:

- минимальная ширина оценивается как `min_width_cells × медианный поперечный размер КЭ`;
- используется безопасная рабочая семантика: одна деталь должна быть не слабее каждого
  накрытого ею требуемого КЭ;
- для одинакового шага проверяется точный зазор на величину шага; для разных шагов
  валидатор до ответа конструктора консервативно требует больший шаг и предупреждает об
  использованном допущении;
- spatial baseline аппроксимирует разрешённую область плитками, пересекающими геометрию
  `KLEENKA`; точный векторный контур и семантика колонн/капителей пока отсутствуют;
- для произвольного DXF без `.shk` и без явно выбранной таблицы неизвестны диаметр и
  шаг, поэтому `DemandMap` построить можно, а `LayoutProblem` для детализации — нельзя;
- для четырёх DXF плиты нуля доступна таблица `plate-zero-d12-v1`; она проверяет шесть
  границ шкалы, связывает ACI по порядку и помечает результат как `mvp_assumption`.

Общий builder/postprocessor приводит ширину к `k × step`, вычисляет `bar_count = k + 1`,
применяет политику анкеровки, раздвижки и раскроя. `build_zone(cell_ids)` сохранён для старых baseline, а
`build_zone_from_bbox()` принимает пространственный прямоугольник без обратного
превращения в bbox группы КЭ. Независимый hard-валидатор разрешает overlap
`demand_bbox` зон одного направления, считает площадь объединения без двойного зачёта и
разрешает покрыть один КЭ объединением нескольких прямоугольников, каждый из которых
имеет достаточный уровень. Отдельный этап детализации разворачивает зоны в реальные
линии, добавляет `40d` и пытается обеспечить зазор крайних осей. Его предупреждения не
заставляют алгоритм склеивать прямоугольники.

Для контроля качества GA добавлен внутренний `algorithms/genetic/exact_oracle.py`:
точный перебор малого CandidateSet с общими детализацией и hard-валидатором, но без
GA repair и отбора. Он не регистрируется как новый baseline. Оркестрация сравнений
находится в `application/genetic_oracle.py`, JSON/CSV/HTML — в
`reporting/genetic_oracle.py`, CLI — `scripts/benchmark_genetic_oracle.py`.
Внутренняя `_materialize_genome()` общая с GA; публичные контракты не меняются.
Границы сертификата, лимиты и первая серия описаны в
[`genetic-experiments.md`](genetic-experiments.md#exact-oracle-малых-масок--5-сентября-2026).

Внутренний `genetic/grid_oracle.py` дополняет ограниченный пул всеми допустимыми
сеточными bbox × уровнями, сохраняя исходные кандидаты. Это диагностика полноты
генератора, не новый production-алгоритм. `genetic/candidates.py` и
`genetic/local_search.py` содержат opt-in расширение геометрий и локальные предложения.
Их оркестрация остаётся внутри GA; JSON/HTML и внешние инженерные метки туда не входят.
Исследование и контрольные/отрицательные результаты:
[`ga-research-2026-09-05.md`](ga-research-2026-09-05.md).

Следующий opt-in этап остаётся внутри `genetic-pareto`: `genetic/recombination.py`
расширяет геометрии (с полной переиндексацией атомов покрытия, но сохранением индексов
кандидатов), `genetic/pool_solver.py` выбирает подмножества MILP по epsilon-бюджетам.
SciPy находится в optional extra `solver`, импортируется только при включении профиля.
Ни один из этих модулей не сериализует HTML/JSON и не получает golden. Все исходные
родители сохраняются до общей hard-validation предложений; solver gap не подменяет
её. Метаданные solver и время агрегирует reporting, CLI принимает флаги гибридного
профиля. Подробности: [`ga-hybrid-research-2026-09-06.md`](ga-hybrid-research-2026-09-06.md).

Пространственный индекс КЭ относится только к общему сервису детализации:
`DetailingContext.spatial_index` ускоряет отбор потенциально пересекающихся полигонов,
не меняет формулы и не используется независимым hard-валидатором. Устранение повторного
сбора кэша до/после подбора фаз сохраняет полное восстановление исходных зон при ошибке.
Внутренний эксперимент `pool_solver.budget_order` меняет только порядок промежуточных
epsilon-задач; публичный GA по-прежнему использует `complexity-first`.
Сравнение: [`ga-performance-research-2026-09-06.md`](ga-performance-research-2026-09-06.md).

## Реализованный этап физической модели

Перед solver и уровнем всей плиты общая физическая модель была исправлена:

1. отделить область спроса, установленный продольный интервал и поперечные оси;
2. сделать `40d` заменяемой политикой и добавить проектный каталог длин;
3. разрешить overlap `demand_bbox`, не допускать двойной зачёт площади и отдельно
   диагностировать совпадения стержней/крайних осей;
4. независимо пересчитывать покрытие, длину и массу;
5. покрыть правила синтетическими тестами X/Y и регрессией реального DXF;
6. добавить `row-run-greedy` и `strip-profile-dp` как два простых MVP-алгоритма;
7. рисовать в SVG как envelope зон, так и реальные линии стержней.

Все семь пунктов реализованы и покрыты общими поведенческими и синтетическими X/Y-тестами.
После MVP можно возвращаться к `PlateProblem`/`PlateSolution`, полному `CandidateSet`,
Парето-фронту и CP-SAT/MILP. Все методы сохраняют прежнюю границу `LayoutOptimizer` и
проходят один общий валидатор.

Внешняя проверка алгоритма не входит в оптимизатор. Пакет `golden` извлекает из готового
PDF плиты нуля 79 позиций, 1019 физических стержней и 3177,64 кг, а reporting строит
отдельный автономный HTML. После конвертации DWG→DXF eval-харнесс добавит сравнение bbox
и IoU, не меняя интерфейс алгоритмов. Подробности: [`golden-case.md`](golden-case.md).

Порядок работ и критерии локальной демонстрации собраны в
[`mvp-roadmap.md`](mvp-roadmap.md).

## Внешняя калибровка DXF / Revit

`application/revit_dxf_calibration.py` сопоставляет Mosaic после обычного ingest
с read-only отчётом `qm_revit_cad.py`. Autodesk API остаётся только в pyRevit-адаптере,
ядро и GA его не импортируют. Checker проверяет все вершины и исходные КЭ, используя
записанное преобразование без подгонки. CLI отвечает за пути, хеши и новый JSON.
Результат — диагностический `revit-dxf-calibration/v1`, не PlacementContext для apply
и не изменение публичных models.py/v1/v2. Успех не снимает export/engineering-гейты;
Z стержней по-прежнему должен поступать от граней host, не от плоскости CAD.

В pyRevit 0.5.1 метаданные и ограниченное сравнение двух API-чтений вынесены в
`qm_cad_diagnostics.py`. Основной symbol-read остаётся единственным источником
доверенных треугольников checker: альтернативный instance-read и меши неизвестных
слоёв только диагностические. Совпадение всех мешей двух чтений не удостоверяет
`KLEENKA` и не открывает калибровку/размещение. Контракт ядра и метрики GA не меняются.

10 сентября добавлен **отдельный source-authority путь**, не fallback старого checker:
`application/revit_source_binding.py` + `scripts/verify_revit_source_binding.py`.
Требования читаются из явно выбранных DXF/SHK; весь сохранённый CAD mesh-снимок
проверяется с явным числом геометрических копий. Слои/цвета CAD не удостоверяются.
Оба пути используют общий численный `_compare_triangles`, но имеют разные входные
условия и названия успеха. `source_snapshot_matches` не является live PlacementContext
и не открывает экспорт. [Контракт и реальный 051](revit-source-binding-2026-09-10.md).

## Исследовательская проверка составного покрытия

`contracts/composite_coverage.py` хранит отдельный результат проверки, а
`services/composite_coverage.py` использует общий составной checker и геометрический
union. Область обслуживания периодических осей и отношение упорядоченных recipes
явно версионированы как research-профиль; готовность к размещению не утверждается.
В `application/composite_layout_review.py` находятся валидация нового JSON-входа,
адаптация Mosaic, вызов детализации и проверка заданных зон; в CLI — пути/хеши/запись.
На этапе этой проверки solver не добавлялся. Старый LayoutProblem продолжает отклонять
несовместимые составные задачи; v2/Core Trial payload не изменён.
Детали: [составное покрытие](composite-coverage-research-2026-09-09.md).

## Составной поиск и host — 10 сентября

Отдельный `CompositeSearchProblem` не адаптируется к одиночному `LayoutProblem`.
`algorithms/composite_pool.py` предлагает геометрию; `services/composite_windows.py`
выбирает обслуживающее периодическое окно, общие detailing/coverage дают стоимость
и проверку. `services/finite_cover.py` решает конечную матрицу полного покрытия КЭ
при ограничениях числа зон; итог повторно проверяется независимым coverage-сервисом.
Оптимизатор не сериализует JSON и не импортирует Autodesk. Его не регистрируют в
старом registry: там несовместимый однокомпонентный контракт.

`prepare_composite_review`/`build_review_zones` — общая граница конфигурации review,
поиска и host-аудита. `application/optimize_composite_layout.py` оркестрирует сервисы,
CLI отвечает за файлы, ограничения размеров, хеши и запрет перезаписи.
`contracts/host.py`/`services/composite_host.py` проверяют все компоненты по явному
призматическому envelope, не подрезая оси. Из Reference Probe берутся совпадающие
верхние/нижние прямоугольные контуры; полнота Solid остаётся неизвестной.
Опциональный строгий host-профиль отбрасывает нарушающие кандидаты/раскладки;
необходимое условие размещения 40d способно остановить поиск **до** solver.
Успех этой ограниченной проверки не отменяет ни одного неизвестного инженерного гейта.
Контракт v2 и тестовые pyRevit-команды не менялись.

[Реальный эксперимент и 67 несовместимых краевых КЭ](composite-search-host-2026-09-10.md).

Согласованный частичный профиль `CompositeSearchProblem.boundary_mode="interior-exceptions"`
использует `services/composite_interior.py` для фиксации внутреннего прямоугольника,
ID целевых целых КЭ и полного списка исключений. Сам `DemandMap` не сокращается:
детализация, масштаб минимальной ширины и окончательный coverage используют оригинал.
`finite_cover.py` принимает конфликтные пары и независимые лимиты массы/физических
стержней; длина отсекается по общей детализации, а не фиктивным раскроем. Конфликты
вычисляет оптимизатор через ту же геометрию, что и host-checker. После MILP выполняется
повторная валидация всех ограничений и полного исходного спроса.
Application не заменяет `needs_revision` частичного результата на полный `pass`:
отдельно показывает target coverage, исходный остаток, неизвестную полную массу,
непроверенную сопоставимость с инженером, товарные длины и позиции спецификации.
Подробнее: [границы контракта и проверка качества](composite-interior-research-2026-09-10.md).

11 сентября добавлен второй solver того же `CompositeSearchProblem/Result`:
`composite_partition.py`. `services/composite_grid.py` создаёт консервативные атомы
из пересечений исходных полигонов и сохраняет площадь каждого целевого КЭ.
`check_composite_zone_coverage_geometry` — общая часть coverage, используемая и в
DP-кандидатах, и в полном checker. `CompositeDetailingContext` кеширует только
статистику исходного demand; независимый итоговый валидатор не доверяет этому кешу.
API/CLI явно выбирают `whole-cell-pool` либо `fragment-grid`; неподходящие параметры
CLI отклоняются. Registry старого GA и pyRevit-команды не изменены.
Host-отчёт дополнен ограниченной диагностикой проекций межзонных конфликтов при
неизвестном Z: `none_detected` не даёт 3D-pass, превышение лимита даёт `not_checked`.
Детали: [фрагментный эксперимент](composite-fragments-research-2026-09-11.md).

Следующий слой эксперимента — `composite_joint_depths.py`, отдельный CSP назначения
неизвестных глубин уже выбранных наборов. Граф `composite_joint_graph.py` представляет
геометрические ограничения, но не заменяет окончательные `evaluate_composite_coverage`
и `evaluate_composite_host`. Все покрытие/масса должны совпасть с исходной точкой;
известные глубины не двигаются. У результата всегда `placement_eligible=false`.
Публичные `models.py` и review-input/v1 не меняются; JSON `composite-joint-depth-research/v1`
отдельно содержит исходный конфиг, явный каталог/источник гипотезы, назначенные глубины
и полные параметрические `proposed_zones`. Это не команда Revit apply и не новое
production-правило укладки. Прочность при перемещении целых стержней остаётся непроверенной.
[Конкретная проверенная гипотеза](composite-joints-research-2026-09-11.md).

## Физическая подгонка к рабочему Solid — 14 сентября

`services/physical_host_fit.py` получает типизированные PhysicalBar/PhysicalSourceBar
и OrthogonalSolidHost в одной системе координат. Через точные непрерывные интервалы
материала вычисляет допустимые начала стержня с cover и полными40d. Непригодный
стержень возвращается неизменным с причиной. Никакой сериализации/Revit API внутри.

`application/physical_host_recovery.py` переводит координаты на границе и оркестрирует
подгонку полной партии, независимые source/stock/host-проверки и отбраковку новых
пар пересечений по стабильным физическим IDs. Публичный LayoutZone не заменяется
исполнительными группами Rebar. Не найденное размещение не разрешает частичный apply.
`prepare_revit_assistant.py` подключает этап явно заданным host-профилем, пишет хеши,
новые артефакты и диагностический ZIP. `reporting/assistant_handoff.py` формирует
`RESULT.md`, отдельно показывает расчётное покрытие и непройденные физические гейты.
Отчёт с `blocked_working_host` возвращает код2 и не означает инженерской готовности.

### Замороженные обязательства по исходным КЭ — 14 сентября

`services/fe_host_repair.py` определяет обязательства по полной исходной партии
до поиска и независимо проверяет фактическое обслуживание после изменения концов.
Общее геометрическое покрытие вынесено в `opening_relocation.coverage_from_offers`;
прежний режим переноса сохраняет свой контракт и свою политику.

`algorithms/fe_host_repair.py` получает PhysicalBar, заранее определённые обязательства
и OrthogonalSolidHost. Он не перечитывает DXF, не формирует JSON, не переопределяет
спрос и не объявляет результат пригодным к размещению. CLI
`experiment_fe_host_repair.py` отвечает за восстановление входов/SHA256, вызов поиска
и свежей проверки, исключительную запись нового `fe-host-repair-experiment/v1`.

Этот opt-in эксперимент не регистрируется как старый LayoutOptimizer: его результат
ещё требует восстановления прямоугольных LayoutZone и проверки минимальной ширины.
Старые zone-сертификаты и Revit-пакеты не переиспользуются для изменённых концов.
[Серия и ограничения](fe-host-repair-2026-09-14.md).

### Полные зоны без обязательного симметричного положения — 14 сентября

Поперечные кандидаты выделены в `services/composite_zone_translation.py`, а точное
пересечение допустимых общих продольных сдвигов — в `services/composite_host_fit.py`.
Ни один сервис не сериализует данные и не обращается к Revit. Вместо смещения
глобальной сетки переводится окно выбора осей; вместо укорачивания арматуры
распределяется доступный запас длины. Исходный DemandMap неизменен.

Оркестратор `application/asymmetric_zone_shift.py` получает типизированные полные
четыре направления, зоны, настройки укладки и OrthogonalSolidHost. Независимые
coverage/stock/host/collision-сервисы проверяют результат конечного поиска.
CLI `experiment_asymmetric_zone_shift.py` восстанавливает свежий исходный спрос,
проверяет цепочку SHA256 и настройки исходного конвейера, формирует новые zone-drafts
через прежний экспортёр. Опциональная последующая нормализация использует старый
общий сервис и повторную проверку рабочего Solid; новый пакет не отправляется в Revit.

Публичный `models.py` и старый прямоугольный host API не менялись. Проверка GA
подключается отдельно через constructor-only `candidate_guard`; по умолчанию
результаты/случайная последовательность прежнего GA сохранены.
[Ограничения и воспроизведение](asymmetric-zone-shift-2026-09-14.md).

### Физические оси и замена коллизий после FE-repair — 14 сентября

`services/fe_transverse_repair.py` и `services/collision_replacement.py` — отдельные
независимые валидаторы research-профилей. Первый фиксирует весь инвентарь и концы,
разрешая только проверенные поперечные оси; второй применяет явные типизированные
операции слияния/бокового удвоения. В обоих профиль обслуживания замораживается по
каждой положительной достаточной части исходного КЭ каждого владельца **до**
операций, а не по устаревшему зонному bbox после изменений. Слабые As не суммируются.
После операций проверяется вся партия, исходный FE-спрос, фон, stock и host.

Конечные экспериментальные поиски находятся в CLI
`experiment_fe_transverse_repair.py` и `experiment_collision_replacement.py`;
это opt-in research orchestration, не новый зарегистрированный LayoutOptimizer
и не production-замена штатного конвейера. Общий read-only
`scripts/research_fe_inputs.py` восстанавливает источник и проверяет происхождение
предыдущих изменений. Сами геометрические сервисы не читают JSON, не сериализуют
результат и не обращаются к Revit. Будущий production-поиск должен перейти в
`optimization/algorithms/` после согласования инженерного профиля.

`host_conflict_diagnostics.py` объясняет остаточные нарушения и выдаёт необходимое
bbox-препятствие **текущей** модели прямых 40d. Диагностика не меняет source/политику
и не присваивает разрешение размещения. [Серия и пределы вывода](edge-resolution-2026-09-14.md).

### Криволинейная геометрия и независимая общая проверка — 14 сентября

`contracts/shaped_physical.py` хранит явные Line3D/Arc3D отдельно от публичного
LayoutZone. `services/shaped_geometry.py` проверяет всю кривую с защитным слоем,
настоящую длину и геометрический узел; `shaped_collisions.py` разделяет доказанные
пересечения и неопределённость. Оба статуса запрещены для исправленных стержней.
`algorithms/shaped_global_repair.py` только предлагает конечные кандидаты;
`services/shaped_global_coverage.py` независимо восстанавливает каноническую
геометрию, проверяет неизменный исходный FE и полный инвентарь. CLI
`experiment_global_shaped_repair.py` отвечает за свежие входы, SHA и отчёт,
но не создаёт структурный Revit-пакет. Инженерное согласование не подменяется
успехом программной проверки.

Контракты переноса спроса находятся отдельно в `contracts/demand_transfer.py`.
Трансформация и её проверка не меняют исходные DemandMap; сертификат сохраняет
происхождение каждого фрагмента. Интеграл стали, геометрическое покрытие и
инженерная эквивалентность — разные утверждения.

`algorithms/shaped_joint_repair.py` — отдельный эксперимент одновременного выбора
целых стержней из конечного пула. MILP выбирает ровно одного кандидата на исходный
стержень. После каждого решения пересчитываются полные полигоны исходного FE и
все 3D-пары; непокрытые участки добавляют ограничения покрытия, новые конфликты —
запреты пар. Проверка отдельных точек служит только внутреннему поиску, не гейтом.
Возвращается лишь полный проверенный промежуточный вариант либо полная исходная
партия. Общий независимый `shaped_global_coverage.py` повторно проверяет выход;
новый метод не заменяет его своим MILP-статусом. Невошедшие в пул варианты и лимиты
поиска явно протоколируются, глобальная оптимальность не заявляется.

Опциональное `allow_length_reassignment` разрешает обмен длинами внутри того же
направления, диаметра и класса стали. MILP сохраняет **количество каждой длины**
в партии, не только сумму метров; независимый валидатор повторно сравнивает весь
инвентарь. Состав исходных владельцев, диаметры и FE-спрос не меняются. Каноническая
U-геометрия восстанавливается с выбранной длиной, а продольный сдвиг имеет отдельный
явный предел. По умолчанию обмен выключен. Эта постановка не разрешает укорачивать
всю партию, добавлять отходы, переносить спрос или пропускать проверку коллизий.
CLI: `experiment_global_shaped_repair.py --solver joint --reassign-lengths`.
### Самостоятельный режим разбиений и выбор точки — 18 сентября

`search_mode="zone-merge"` входит через тот же application API, но вызывает
отдельный `optimization/algorithms/composite_merge.py`; прежний поиск позиций
не изменяется. Алгоритм предлагает зоны, общая детализация и независимый
checker оценивают их, `zone_tradeoff.py` объединяет четыре направления и
находит геометрическое колено. `learning/zone_preference.py` принимает только
полный проверенный отчёт и малый bundle коэффициентов: он ранжирует индексы
существующих точек и не создаёт геометрию. `engineering_preference` в web
появляется лишь при `status="available"`; отсутствие модели не меняет
результат поиска. [Контракты и ограничения](zone-partition-experiment-2026-09-18.md).
