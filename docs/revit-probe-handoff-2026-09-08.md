# Сообщение для повторного Creation Trial

Отправить с `artifacts/revit_probe/qmonitoring-revit-probe-0.2.2.zip`.

---

Спасибо, отчёты получил. Теперь видно, что мешала камера 3D-вида: моя проверка
ошибочно считала её физическим препятствием. Эскиз уже распознаётся правильно.
В этом запуске стержни ещё не создавались, модель скрипт не менял.

Добавил проверку категории камеры, прикладываю архив 0.2.2. Ничего в модели удалять,
скрывать или перемещать не нужно. Закрой Revit, замени QMonitoring.extension в той же
подключённой папке и после перезапуска запусти только Creation Trial на тестовой копии.
В первой строке журнала должна быть версия 0.2.2. Reference Probe повторять не нужно.

Пришли новый JSON Creation Trial и скриншот окна результата. После создания тест
по-прежнему обязательно отменяет изменения, даже при успехе; RVT сохранять не нужно.
Если появится ошибка, пришли полный журнал. При неподтверждённом откате закрой
тестовую копию без сохранения.

---

Разработчику: 0.2.1 имеет `blocked_preflight / not_started`; создание и откат ещё
не подтверждены в Revit. В 0.2.2 исключается только встроенная `OST_Cameras`, не имя
`{3D}`, ID конкретного элемента или все категории Model. Ожидаемый результат следующего
теста — `passed_rolled_back`, не инженерная приёмка и не разрешение заполнять плиту.
