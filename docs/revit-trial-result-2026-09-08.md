# Первый успешный тест создания в Revit

Источник: `revit_info/022/qmonitoring-creation-trial-20260908-163832-016000.json`,
кнопка 0.2.2, реальный Revit 2024 / IronPython 2.7.12.

Результат — **passed_rolled_back**. Создан и прочитан обычный Rebar-набор:

| Проверка | Фактический результат |
| --- | --- |
| Стержни | 9 × Ø25, тип 25 A500, без крюков |
| Длина по осям | 3900 мм каждый |
| Шаг | 96,875 мм, NumberWithSpacing |
| Верхний защитный слой | 25 мм до тела стержня |
| Сверка | 55 из 55; концы осей совпали в пределах численного шума |
| Откат | Подтверждён; состав ID и ручной эталон восстановлены |

Размеры дополнительно пересчитаны по самим концам линий, а не только проверены
по готовому статусу JSON. Ошибок нет, IsModified=false до и после теста.
Reference Probe тоже прошёл: collected / matches / 0 issues.

Это подтверждает работу создания, фактического считывания и отката на этой модели.
Commit и состояние после его выполнения не проверены. Не проверены также привязка
исходного DXF, фоновые фазы, порядок X/Y и покрытие всей плиты. Набор после теста
не остаётся в модели; placement_eligible=false не снимается.

Новый архив для повторения того же теста не нужен. Следующий предлагаемый этап —
передача параметров одной зоны через JSON и отдельная проверка завершения транзакции
на копии, с последующим контролем фактических осей и восстановлением модели.
Оставление набора для визуальной проверки потребует отдельного явного режима.

## Короткий ответ специалисту

Спасибо, теперь тест прошёл полностью! Скрипт создал все девять стержней, считал
их оси и корректно отменил создание. Длина, шаг и защитный слой совпали, твой эталон
не изменился. Повторять этот запуск больше не нужно. Следующим этапом хочу проверить
передачу параметров одной зоны через JSON и завершение транзакции в Revit —
по-прежнему на копии, без автоматического заполнения всей плиты.
